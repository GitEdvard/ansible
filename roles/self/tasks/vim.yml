- name: fetch vim from git
  git:
    repo: https://github.com/vim/vim.git
    dest: "{{ sources_other }}/vim"
  tags: skip
  when: false

- name: Uninstall vim
  apt:
    name: vim
    state: absent

- name: intall compiled vim with support for ruby, python etc. 
  apt:
    name: vim-nox
  tags: skip
  when: false

- name: fetch nvim executable
  become: yes
  become_user: edvard
  get_url:
    url: https://github.com/neovim/neovim/releases/download/nightly/nvim.appimage
    dest: /home/edvard/bin/nvim
    mode: '0740'

- name: Create config directory for nvim
  file:
    path: /home/edvard/.config/nvim
    state: directory

- name: Deploy init.vim file 
  copy:
    src: init.vim
    dest: /home/edvard/.config/nvim/init.vim
  
- name: Fetch Jeremy Kahns dotfiles repo
  git:
    repo: https://github.com/jeremyckahn/dotfiles.git
    dest: "{{ sources_test }}/dotfiles"

- name: Fetch Jeremy Kahns vim-docker-env repo
  git:
    repo: https://github.com/jeremyckahn/vim-docker-env.git
    dest: "{{ sources_test }}/vim-docker-env"

#- name: run make distclean for idempotency
#  make:
#    chdir: "{{ sources_other }}/vim/src"
#    target: distclean
#
#- name: configure the compilation
#  shell: "{{ sources_other }}/vim/src/configure --with-features=huge --enable-rubyinterp=yes "
#
#- name: run make in vim repo
#  make:
#    chdir: "{{ sources_other }}/vim/src"
#
#- name: make install vim
#  make:
#    chdir: "{{ sources_other }}/vim/src"
#    target: install

